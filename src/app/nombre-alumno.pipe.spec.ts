import { NombreAlumnoPipe } from './nombre-alumno.pipe';

describe('NombreAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
